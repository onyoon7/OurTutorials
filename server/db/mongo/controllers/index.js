import categoryTrees from './categoryTree';
import links from './links';
import courses from './courses';
import users from './users';

export { users, categoryTrees, links, courses };

export default {
  categoryTrees,
  users,
  links,
  courses
};
